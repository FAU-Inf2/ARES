class AresInput {

  example() {
    //# match (modified) {
    //# use (ARES0, verbose, 1)
    this.init(verbose);
    updateValue();
    for (k = 0; k < 10; k++)
      //# use (ARES1);
    if (foo != null) {
      foo.someMethod(42);
    }
    //# choice {
    //# case {
    System.out.print(foo);
    //# }
    //# case {
    this.print(foo);
    //# }
    //# }
    this.shutdown();
    //# }
}
}
