class AresInput {

  example() {
    //# match (original, (k)) {
    //# wildcard expr(ARES0, verbose, 1)
    this.init(verbose);
    this.shutdown();
    updateValue();
    k = 0;
    while (k < 10) {
      //# wildcard stmt(ARES1);
      k++;
    }
    foo.someMethod(42);
    //# }
  }

}
